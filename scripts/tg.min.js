const canvas=document.getElementById("bg"),ctx=canvas.getContext("2d");let w,h;function resize(){w=canvas.width=window.innerWidth,h=canvas.height=window.innerHeight}window.addEventListener("resize",resize),resize();const numStars=250,stars=[];for(let t=0;t<numStars;t++)stars.push({x:Math.random()*w,y:Math.random()*h,size:2.5*Math.random(),speed:.25+.5*Math.random()});function animate(){ctx.clearRect(0,0,w,h);for(let t=0;t<numStars;t++){const i=stars[t];i.y+=i.speed,i.y>h&&(i.y=0-i.size,i.x=Math.random()*w),ctx.beginPath(),ctx.arc(i.x,i.y,i.size,0,2*Math.PI),ctx.fillStyle="#fff",ctx.fill()}requestAnimationFrame(animate)}animate();